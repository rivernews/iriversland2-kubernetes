terraform {
  # please use tfenv if you're not in this version interval
  # note that tfstate is binded with tf version
  # so be sure to use the specified version whenever possible
  required_version  = "<=0.12.18, >= 0.12.18"
}
